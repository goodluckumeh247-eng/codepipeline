#!/bin/bash
dnf update -y                    # Update installed packages
dnf install -y httpd             # Install Apache web server
systemctl start httpd            # Start the Apache service
systemctl enable httpd           # Enable Apache to start on boot
echo "<h1>EC2 Welcome Page!!!</h1>" > /var/www/html/index.html
